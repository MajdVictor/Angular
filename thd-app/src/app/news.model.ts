/**
 * News Interface that defines the structure of News objects
 */
export interface News {
  /**
   * string variable that holds the title of news article
   */
  title: string;

  /**
   * string variable that holds the content of news article
   */
  content: string;
}
