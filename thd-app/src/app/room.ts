/**
 * Room interface that describes the structure of Room objects
 */
export interface Room {
  /**
   * string variable that holds the id of the room
   */
  id: string;

  /**
   * string variable that holds the description of the room
   */
  description: string;
}
